import type { Config } from "tailwindcss";

/** Theme and plugins are defined in `app/globals.css` (Tailwind v4). Path kept for shadcn/cli. */
const config = {} satisfies Config;

export default config;
